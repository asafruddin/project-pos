---
scope: POS Apps Phase 1 — coffee-shop Instant Checkout + Offline Mode
purpose: build-substrate for solo builder + coding agents (Phase 1)
altitude: initiative → features (Phase 1 platform slice)
updated: 2026-08-13T21:20
---

- (constraint) Inherited from final PRD: coffee-shop Phase 1; Instant Checkout + Offline Mode; Local Database + Sync; Sale complete only after payment+Receipt; PWA Cashier + thin Dashboard; FR-1..32; no SaaS growth gate; no KDS/multi-branch.
- (event) Create run bound. Input: prd-pos-apps-2026-08-05 (final). Greenfield (no app code yet). Awaiting working mode + purpose/audience.
- (decision) Working mode: Fast path. Purpose ASSUMPTION: build-substrate for Achmad + coding agents implementing Phase 1; spine is primary deliverable (optional human rendering later).
- (decision) Paradigm: local-primary offline-first — Cashier Local Database is source of truth for complete-but-unsynced Sales; server is authority for catalog + synced Sales + Dashboard Stock.
- (decision) Starter ASSUMPTION: pnpm + Turborepo (create-turbo) monorepo; TypeScript everywhere. Alts: single Next-only (soft Sync boundary), Nx (heavier).
- (decision) Apps ASSUMPTION: apps/cashier Next.js 16 PWA, apps/dashboard Next.js 16, apps/api Hono on Node. Alt one Next app rejected for Offline Mode clarity.
- (decision) Local storage ASSUMPTION: IndexedDB via idb for Local Database + Sync outbox. Alt SQLite WASM deferred.
- (decision) Server DB ASSUMPTION: PostgreSQL + Drizzle ORM. Alt SQLite server deferred.
- (decision) AD Sale ownership: only complete Sales (payment+Receipt) enter Sync outbox; incomplete never decrement server Stock.
- (decision) AD Sync: client-generated sale_id; API Sync idempotent on sale_id; retry-safe; no CRDT.
- (decision) AD Dependency: apps→packages only; packages never import apps; domain package zero UI/framework/DB imports.
- (decision) AD Stock: server mutates Stock only on accepted complete Sale (online or Sync). Dashboard reads server only.
- (decision) AD Auth: Account Login via API; POS PIN against cached Local Database material after first online login.
- (version) Pinned seed verified npm 2026-08-06: next@16.3.0 turbo@2.10.8 hono@4.13.0 drizzle-orm@0.45.2 idb@8.0.3
- (assumption) Deploy ASSUMPTION: Vercel for Next apps; API Node host + managed Postgres — exact host deferred.
- (question) Open: Postgres host provider; ESC/POS library choice — defer to stories.
- (event) Fast-path draft decisions logged; distilling ARCHITECTURE-SPINE.md
- (event) Fast-path ARCHITECTURE-SPINE.md distilled (draft). Awaiting user correction of ASSUMPTIONs before Finalize.
- (decision) API stack changed: NestJS 11.x replaces Hono. Reason: POS will grow beyond thin Sync API; Nest modules (Auth, Catalog, Sales/Sync, Stock) scale better. Invariants AD-1..8 unchanged.
- (change) Removed Hono ASSUMPTION; apps/api = NestJS + Drizzle + Postgres.
- (version) Pinned apps/api: @nestjs/core 11.1.x (npm view 2026-08-06).
- (event) Finalize started: distill check → reconcile PRD → reviewer gate → triage → close.
- (decision) Finalize autofix AD-1: all Sales local-first then Sync (no direct API Sale create). AD-4: AcceptCompleteSale single Stock path + AdjustStock separate. AD-9 catalog pull. AD-10 price snapshot. AD-11 roles. Sync DTO in packages/types. Ops envelope added. Serwist only.
- (decision) Deferred: oversell-on-Sync policy fail-closed ASSUMPTION; exact cloud providers; ESC/POS lib.
- (event) spine finalized
- (event) Update run: Phase 2 PRD (final 2026-08-13). Fast path (user: pipeline until create-story). Resume existing workspace; AD-1..11 IDs stable.
- (constraint) Inherited Phase 1: local-primary offline-first; AD-1..11; cashier PWA + dashboard + Nest API; no KDS; no CRDT.
- (decision) AD-12 Media isolation: only apps/api MediaService talks to Cloudinary 2.10.x; cashier never calls CDN on Menu/Checkout/Receipt/Sync after catalog refresh; image bytes/cache in local-db.
- (decision) AD-13 after 2A cutover: Stock Ledger is server qty SoT; AcceptCompleteSale/AdjustStock/GoodsReceipt/Return post movements; products.stock_qty is projection. Opening movement from Phase 1 qty.
- (decision) AD-14 Cashier durable outbox extends to Shift/Void/queued Customer create (local-first). Returns lookup and Loyalty redeem remain online-first API.
- (decision) AD-15 Nest domain modules: Catalog, Inventory, Purchasing, Sales, Customers, Promotions, Reports, Stores, Identity. Catalog/Sales must not import Cloudinary SDK. packages/domain owns Sale completeness, ledger posting, promo/loyalty eval.
- (decision) AD-16 after 2C: AcceptCompleteSale requires shift_id; Checkout disabled without open Shift. Shift lives in cashier local-db then Sync.
- (decision) AD-17 API enforces resource×action Permissions; UI hide is insufficient. Until 2D, roles remain cashier|catalog_admin (catalog_admin = approver). User admin Dashboard-only.
- (decision) AD-18 Promo/Loyalty/customer-price evaluation lives once in packages/domain; fail-open to catalog/Store price if rules unavailable.
- (decision) AD-19 Phase 1 data is Store #1 + one Register stub. Sale/ledger rows carry store_id. Cashier session bound to one Register. Cross-store offline Sync out.
- (version) Brownfield ratified: next 16.3.0, nest 11.1.x, drizzle 0.45.1, turbo 2.10, serwist 9, pnpm 11.20. Cloudinary npm 2.10.0 (verified 2026-08-13).
- (event) Reviewer autofix: named ledger commands; outbox envelope; Void vs Return vs cancel; movement bucket; Variant product_id; fail-open widened; Cloudinary not in lockfile; UUID v4 kept.
- (event) spine finalized
- (event) Story 4.3 implemented AD-12: cloudinary@2.10.0 in apps/api lockfile; sole SDK import cloudinary.adapter.ts; product_images + media_delete_retries. Cashier image cache remains Story 4.4.
- (event) Story 4.4: cashier catalog refresh caches primary image bytes in IndexedDB v5 via GET .../images/:id/file (MediaService fetch). Menu uses blob/placeholder; checkout/sync still have no CDN. Next: 4.5 stock overview/damaged/low-stock.
- (event) Story 4.5: GET /inventory/overview sums ledger buckets at Store #1; POST .../damaged posts sellable OUT + damaged IN (shared source_id). Dashboard Ikhtisar stok lists Rendah/Habis. Instant Checkout still not hard-blocked. Next: 4.6 Stock Opname (ApplyOpname).
- (event) Story 4.6: Stock Opname draft snapshots sellable SUM; ApplyOpname on approve posts source_type=opname so Overview matches counted. Reject/cancel do not move qty. Dashboard Opname stok is catalog_admin only. Epic 4 done. Next: Epic 5.1 suppliers and PO lifecycle.
- (event) Story 5.1: PurchasingModule suppliers + PO lifecycle (draft→submitted→approved; cancel draft/submitted). Submit/approve do not post stock. received_qty=0 until 5.2 ReceiveGoods. Dashboard Pembelian is catalog_admin only.
- (event) Story 5.2: ReceiveGoods posts sellable STOCK IN (source_type=goods_receipt). Partial → partially_received; full → completed. Over-receive rejected. Invoice ref/payment_status do not move stock. Next: 5.3 hold/park Cart Panel.
- (event) Story 6.3 done: Cash In/Out, FR-78 Expected Cash, close with counted vs difference (warn, not hard-block). Close does not drain Sync. Dashboard Shift review. Migration 0015_shift_cash.
- (event) Story 6.4 done: Day Close finish blocked while Shift open (FR-111); cash summary displays closed Shift snapshots (no second FR-78 formula); FR-24 ack still required. Confirm does not drain Sync.
- (event) Story 6.5 done: split tender cash+Store Credit (FR-110) and fail-open customer/group price (FR-112/AD-18). Sync snapshots line prices (AD-10). Expected Cash uses cash tenders only. Migration 0016_store_credit_prices.
- (event) Story 7.1 done: Loyalty earn-after-Sync + online redeem (FR-82–FR-86). Domain eval fail-open (AD-18). Discount is sale-level (AD-10). Cashier redeem hidden offline. Dashboard program/ledger catalog_admin. Migration 0017_loyalty. Next: 7.2 promotions/coupons/vouchers.
- (event) Story 7.2 done: promotions percent/fixed + one coupon + voucher remaining + manager PIN discount (FR-87–FR-92). Domain eval fail-open (AD-18). Sale-level stack promo→manager→voucher→loyalty (AD-10). Voucher debit online-only; Void restores remaining. Migration 0018_promotions. Next: 7.3 reports and analytics.
